# Direct dependencies — install these, pip resolves the rest.
# Versions are not pinned; use the versions that work with your Python.

# Image processing (webserver)
numpy
numba                # JIT-compiled dither inner loop (NumbaDither)
colour-science       # CAM16-UCS palette LUT — lazy-imported only when used
Pillow
pillow-heif          # HEIC/HEIF support
pillow-avif-plugin   # AVIF support
pillow-jxl-plugin    # JPEG XL support

# Web server
Flask
zeroconf
zstd                 # panel cache compression

# Firmware tools
esptool
pyserial
esp-idf-nvs-partition-gen   # standalone NVS partition generator — lets hokku-config write frame config without a full ESP-IDF install
rich
rich-click

# Runtime utilities
psutil               # auto worker-count (resolve_worker_count) + memory-budget tests
opencv-python-headless
resvg-py           # SVG rasterisation (bundles Rust binary; no system lib required)

# Tests
pytest
pytest-cov
pytest-xdist
tzdata               # IANA timezone data for zoneinfo on Windows (validate_timezone test)

# Dev tools
defusedxml           # safe XML/SVG parsing (replaces xml.etree in user-upload path)
pre-commit
ruff
pyright
